{
  "kind": "Service", 
  "spec": {
    "type": "ClusterIP", 
    "ports": [
      {
        "protocol": "TCP", 
        "targetPort": 9000, 
        "port": 9000
      }
    ], 
    "selector": {
      "app": "minio"
    }
  }, 
  "apiVersion": "v1", 
  "metadata": {
    "namespace": "deploy-dr", 
    "name": "minio-service-dr"
  }
}
